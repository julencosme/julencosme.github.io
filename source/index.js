/**
 * To use jQuery, first install it as a dependency:
 *
 * ```
 * npm install --save jquery
 * ```
 *
 * Then include:
 *
 * ```
 * import $ from 'jquery';`
 * ```
 *
 * At the top every JavaScript file that uses jQuery.
 *
 * To import other files, adhere to the following conventions:
 *
 * ```
 * import './more-javascript/more.js';
 * import 'normalize.css'; // Note this
 *
 * const saySomething = something => {
 *   console.log(something);
 * };
 *
 * saySomething('Something! (index.js)');
 * ```
 */

import './style/style.scss';
